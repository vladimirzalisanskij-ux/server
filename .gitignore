# Never commit the database, backups or logs - they contain player data.
boblox-platform-db.json
boblox-db.json
backups/
*.log
node_modules/

# Game builds ARE committed: Render serves them from /downloads.
# (Each file must stay under GitHub's 100 MB limit.)
